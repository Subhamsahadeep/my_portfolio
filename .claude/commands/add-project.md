---
name: add-project
description: Add a new project to the portfolio and resume
user_invocable: true
---

The user will describe a new project. Your task:

1. Read `src/app/data/projects.ts` and `src/app/data/resume-data.ts`
2. Add the project to `projects.ts` with:
   - title, description, longDescription (if featured), tech array, featured flag
   - Write in SMART style with measurable impact where possible
3. Optionally add a compact entry to `resume-data.ts` projects array
4. Verify build works
5. Summarize what was added

Project details:

$ARGUMENTS
